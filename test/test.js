/* eslint-disable no-unused-expressions */

import DataFilter from '../'
import { expect } from 'chai'

describe('react-data-filter', () => {
  it('should export the DataFilter class', done => {
    expect(DataFilter).to.exist
    done()
  })
})
